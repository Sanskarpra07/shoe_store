<?php
/**
 * --------------------------------------------------------------------------
 * DATABASE CONFIGURATION
 * --------------------------------------------------------------------------
 * Edit these values to match your local MySQL / MariaDB setup.
 * The connection is opened in backend/db.php using these constants.
 * --------------------------------------------------------------------------
 */

$servername = '127.0.0.1';   // Database server host (localhost / 127.0.0.1)
$username   = 'root';        // Database user
$password   = '';            // Database password (empty for local XAMPP)
$dbName     = 'shoe_store_db'; // Name of the shoe store database